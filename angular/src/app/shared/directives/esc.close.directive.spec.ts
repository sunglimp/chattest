import { Esc.CloseDirective } from './esc.close.directive';

describe('Esc.CloseDirective', () => {
  it('should create an instance', () => {
    const directive = new Esc.CloseDirective();
    expect(directive).toBeTruthy();
  });
});
